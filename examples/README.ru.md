# Tires Examples

Подробные примеры конфигурации для типичных сценариев использования.

**🇬🇧 [English version](README.md)**

---

## Краткая справка

### Полный шаблон конфигурации

```json
{
    "IterationLimit": 20,
    "LogLevel": "Information",
    "LogPath": "/var/log/tires/tires.log",
    "TemporaryPath": "tmp",
    "RunInterval": "hourly",
    "ProcessPriority": 2,
    "Tiers": [
        {"target": 90, "path": "/mnt/ssd"},
        {"target": 100, "path": "/mnt/hdd"}
    ],
    "FolderRules": [
        {"PathPrefix": "important", "Priority": 100, "RuleType": "Ignore"}
    ]
}
```

### Ключевые особенности

- **Сохранение файлов**: Права, владелец, временные метки и жёсткие ссылки сохраняются при перемещении
- **Безопасность**: Служба systemd включает NoNewPrivileges, ProtectSystem, ProtectHome
- **Нет внешних зависимостей**: libMono.Unix.so включён во все пакеты
- **Тестирование с MockCapacity**: Тестирование без реальных ограничений диска с помощью `MockCapacity`

---

## Как работают правила

Файлы сортируются по **баллам** и распределяются по уровням по порядку:
1. **Меньший балл** → назначаются **первыми** → попадают на **быстрый уровень (SSD/NVMe)**
2. **Больший балл** → назначаются **позже** → попадают на **медленный уровень (HDD/Архив)**

### SizeRule — Краткая справка

| Reverse | Порядок сортировки | Файлы сначала идут на | Сценарий |
|---------|-------------------|----------------------|----------|
| `false` (по умолчанию) | Малые → Большие | **Малые файлы на SSD** | Держать малые файлы быстрыми |
| `true` | Большие → Малые | **Большие файлы на SSD** | Держать большие файлы быстрыми |

### NameRule — Краткая справка

| Reverse | Файлы на SSD | Файлы на HDD | Сценарий |
|---------|-------------|-------------|----------|
| `false` (по умолчанию) | **Несовпадающие** | Совпадающие | Убрать определённые типы |
| `true` | **Совпадающие** | Несовпадающие | Держать определённые типы быстрыми |

### TimeRule — Краткая справка

| Reverse | Порядок | Файлы на SSD | Файлы на HDD | Сценарий |
|---------|---------|-------------|-------------|----------|
| `false` (по умолчанию) | Старые → Новые | **Старые файлы** | Новые файлы | Архивировать недавние |
| `true` | Новые → Старые | **Новые файлы** | Старые файлы | Держать недавние быстрыми |

**Запомните:**
- **Size** (по умолчанию) → **Малые сначала** → Малые файлы остаются на SSD
- **Size reverse** → **Большие сначала** → Большие файлы остаются на SSD
- **Name** (по умолчанию) → **Несовпадающие сначала** → Совпадающие уходят на HDD
- **Name reverse** → **Совпадающие сначала** → Совпадающие остаются на SSD
- **Time** (по умолчанию) → **Старые сначала** → Старые файлы остаются на SSD
- **Time reverse** → **Новые сначала** → Новые файлы остаются на SSD

---

## Пример 1: Базовая настройка 2 уровней (SSD + HDD)

**Сценарий:** Домашний сервер с быстрым SSD и большим HDD

### Конфигурация

```json
{
    "IterationLimit": 20,
    "LogLevel": "Information",
    "TemporaryPath": "tmp",
    "Tiers": [
        {
            "target": 90,
            "path": "/mnt/ssd"
        },
        {
            "target": 100,
            "path": "/mnt/hdd"
        }
    ]
}
```

### Что это делает

- **SSD (макс. 90% заполнено):** Малые файлы для быстрого доступа
- **HDD (макс. 100% заполнено):** Большие файлы, архивы, медиа

### Перемещение файлов

| Тип файлов | Размер | Куда |
|------------|--------|------|
| Документы | < 100КБ | SSD |
| Конфигурации | < 50КБ | SSD |
| Видео | > 100МБ | HDD |
| Архивы | > 50МБ | HDD |

### Ожидаемый результат

```
/mnt/ssd/
├── documents/      (малые файлы)
├── configs/        (малые файлы)
└── photos/         (недавние, малые)

/mnt/hdd/
├── videos/         (большие файлы)
├── backups/        (большие архивы)
└── old-photos/     (старые, большие)
```

---

## Пример 2: Исключение важных папок

**Сценарий:** Держать критические файлы на их текущем уровне

### Конфигурация

```json
{
    "IterationLimit": 20,
    "LogLevel": "Information",
    "Tiers": [
        {"target": 90, "path": "/mnt/ssd"},
        {"target": 100, "path": "/mnt/hdd"}
    ],
    "FolderRules": [
        {
            "PathPrefix": "important",
            "Priority": 100,
            "RuleType": "Ignore"
        },
        {
            "PathPrefix": "backup",
            "Priority": 90,
            "RuleType": "Ignore"
        },
        {
            "PathPrefix": "critical",
            "Priority": 80,
            "RuleType": "Ignore"
        }
    ]
}
```

### Что это делает

1. **Приоритет 100:** Файлы в `*/important/*` никогда не перемещаются
2. **Приоритет 90:** Файлы в `*/backup/*` никогда не перемещаются
3. **Приоритет 80:** Файлы в `*/critical/*` никогда не перемещаются
4. Все остальные файлы сортируются по размеру (по умолчанию)

### Структура каталогов

```
/mnt/ssd/
├── important/          ← НИКОГДА не перемещается (приоритет 100)
│   ├── config.json
│   └── keys.db
├── backup/             ← НИКОГДА не перемещается (приоритет 90)
│   └── latest.tar.gz
└── documents/          ← Можно перемещать
    └── readme.txt

/mnt/hdd/
├── critical/           ← НИКОГДА не перемещается (приоритет 80)
│   └── database.sql
└── videos/             ← Можно перемещать
    └── movie.mp4
```

---

## Пример 3: Сортировка видео по размеру

**Сценарий:** Держать большие видео на быстром SSD для быстрого редактирования, малые файлы на HDD

### Конфигурация

```json
{
    "IterationLimit": 20,
    "LogLevel": "Information",
    "Tiers": [
        {"target": 90, "path": "/mnt/ssd"},
        {"target": 100, "path": "/mnt/hdd"}
    ],
    "FolderRules": [
        {
            "PathPrefix": "videos",
            "Priority": 50,
            "RuleType": "Size",
            "Reverse": true
        }
    ]
}
```

### Что это делает

- **`Reverse: true`** — Большие файлы сортируются первыми (меньший балл) → **идут на быстрый уровень (SSD)**
- **Результат:** Большие видео на SSD, малые файлы на HDD

### Распределение файлов

| Файл | Размер | Расположение | Почему |
|------|--------|--------------|--------|
| movie_4k.mp4 | 4ГБ | **SSD** | Самый большой, обработан первым |
| movie_720p.mp4 | 500МБ | **SSD** | Большой, обработан рано |
| clip.mp4 | 50МБ | HDD | Меньше, обработан позже |
| preview.mp4 | 5МБ | HDD | Самый маленький, обработан последним |

### Поведение правила Size

```
Reverse: true → Балл = -Размер
- 4ГБ файл: балл = -4000000 (самый низкий) → SSD первым
- 5МБ файл: балл = -5000 (самый высокий) → HDD последним
```

**Хотите наоборот?** Используйте `"Reverse": false` (по умолчанию):
- Малые файлы (5МБ, 50МБ) → SSD
- Большие файлы (500МБ, 4ГБ) → HDD

---

## Пример 4: 3 уровня (NVMe + SATA + Архив)

**Сценарий:** Высокопроизводительная рабочая станция с архивным хранилищем

### Конфигурация

```json
{
    "IterationLimit": 20,
    "LogLevel": "Information",
    "Tiers": [
        {
            "target": 80,
            "path": "/mnt/nvme"
        },
        {
            "target": 90,
            "path": "/mnt/sata"
        },
        {
            "target": 100,
            "path": "/mnt/archive"
        }
    ],
    "FolderRules": [
        {
            "PathPrefix": "node_modules",
            "Priority": 100,
            "RuleType": "Ignore"
        },
        {
            "PathPrefix": "current_project",
            "Priority": 90,
            "RuleType": "Ignore"
        },
        {
            "PathPrefix": "builds",
            "Priority": 50,
            "RuleType": "Time",
            "TimeType": "Modify",
            "Reverse": true
        }
    ]
}
```

### Стратегия уровней

| Уровень | Target | Назначение |
|---------|--------|------------|
| NVMe | 80% | Активные проекты, зависимости, **новые сборки** |
| SATA | 90% | Недавние сборки, ресурсы |
| Архив | 100% | Старые сборки, резервные копии |

### Объяснение правил

1. **node_modules (Ignore):** Никогда не перемещать npm пакеты
2. **current_project (Ignore):** Держать активную работу быстрой
3. **builds (Time + Reverse):** **Новые сборки → NVMe первыми**, старые сборки → архив

### Ожидаемое распределение

```
/mnt/nvme/
├── node_modules/     ← Никогда не перемещается
├── current_project/  ← Никогда не перемещается
└── recent_builds/    ← Новые сборки (Reverse: true - новые файлы первыми)

/mnt/sata/
├── resources/        ← Средний доступ
└── builds/           ← Недавние сборки (последняя неделя)

/mnt/archive/
├── old_builds/       ← Старые сборки (обработаны последними)
└── backups/          ← Исторические данные
```

### Правило Time для сборок

С `"RuleType": "Time", "Reverse": true`:
- **Новые сборки** (изменены сегодня) → низкий отрицательный балл → **NVMe первыми**
- **Старые сборки** (изменены месяцы назад) → высокий отрицательный балл → **архив последним**

---

## Пример 5: Сортировка по паттерну файла

**Сценарий:** Переместить определённые типы файлов на соответствующие уровни

### Конфигурация

```json
{
    "IterationLimit": 20,
    "LogLevel": "Information",
    "Tiers": [
        {"target": 90, "path": "/mnt/fast"},
        {"target": 100, "path": "/mnt/slow"}
    ],
    "FolderRules": [
        {
            "PathPrefix": "media",
            "Priority": 60,
            "RuleType": "Name",
            "Pattern": ".mp4"
        },
        {
            "PathPrefix": "projects",
            "Priority": 50,
            "RuleType": "Name",
            "Pattern": ".psd"
        },
        {
            "PathPrefix": "documents",
            "Priority": 40,
            "RuleType": "Name",
            "Pattern": ".pdf"
        }
    ]
}
```

### Что это делает

**Поведение по умолчанию (Reverse: false):** Несовпадающие файлы получают балл 0, совпадающие получают балл 1

| Паттерн | Приоритет | Балл несовпадения | Балл совпадения | Файлы на быстром | Файлы на медленном |
|---------|----------|-------------------|-----------------|------------------|-------------------|
| .mp4 | 60 | 0 | 1 | **Другие файлы** | .mp4 файлы |
| .psd | 50 | 0 | 1 | **Другие файлы** | .psd файлы |
| .pdf | 40 | 0 | 1 | **Другие файлы** | .pdf файлы |

### Результат

```
/mnt/fast/
├── source/           ← .cpp, .h файлы (не совпадают ни с одним паттерном)
├── configs/          ← .json, .yaml (не совпадают ни с одним паттерном)
└── cache/            ← Временные файлы (не совпадают ни с одним паттерном)

/mnt/slow/
├── media/            ← .mp4 файлы (совпадают с приоритетом 60)
├── projects/         ← .psd файлы (совпадают с приоритетом 50)
└── documents/        ← .pdf файлы (совпадают с приоритетом 40)
```

### Поведение правила Name

```
Reverse: false → Совпадающие файлы получают балл 1, несовпадающие получают 0
- .cpp файл: балл = 0 (самый низкий) → быстрый уровень первым
- .mp4 файл: балл = 1 (самый высокий) → медленный уровень последним
```

**Хотите держать совпадающие файлы быстрыми?** Используйте `"Reverse": true`:
- Совпадающие файлы (балл -1) → быстрый уровень
- Несовпадающие файлы (балл 0) → медленный уровень

---

## Пример 6: Сортировка по времени

**Сценарий:** Держать старые файлы быстрыми, переместить недавние в архив (или наоборот)

### Конфигурация

```json
{
    "IterationLimit": 20,
    "LogLevel": "Information",
    "Tiers": [
        {"target": 90, "path": "/mnt/fast"},
        {"target": 100, "path": "/mnt/slow"}
    ],
    "FolderRules": [
        {
            "PathPrefix": "documents",
            "Priority": 50,
            "RuleType": "Time",
            "TimeType": "Modify"
        },
        {
            "PathPrefix": "logs",
            "Priority": 40,
            "RuleType": "Time",
            "TimeType": "Access",
            "Reverse": true
        }
    ]
}
```

### Опции TimeType

| Значение | Описание |
|----------|----------|
| `Modify` | Время последнего изменения |
| `Access` | Время последнего доступа |
| `Change` | Время последнего изменения метаданных |

### Что это делает

**Оценка по времени:** Старые файлы = меньшая временная метка = меньший балл → обрабатываются первыми

1. **documents (Modify, по умолчанию):** Старые файлы имеют низкую метку → **старые файлы → быстрые**, новые → медленные
2. **logs (Access + Reverse):** Новые файлы получают отрицательный балл → **недавние → быстрые**, старые → медленные

### Перемещение файлов по возрасту

```
documents/ (Reverse: false - старые файлы первыми):
  old_doc.txt      → /mnt/fast/   (изменён 30 дней назад, самая низкая метка)
  last_week.txt    → /mnt/fast/   (изменён 7 дней назад)
  yesterday.txt    → /mnt/slow/   (изменён 1 день назад)
  today.txt        → /mnt/slow/   (изменён сегодня, самая высокая метка)

logs/ (Reverse: true - новые файлы первыми):
  access.log       → /mnt/fast/   (доступ сегодня, самый низкий отрицательный балл)
  recent.log       → /mnt/fast/   (доступ вчера)
  old.log          → /mnt/slow/   (не accessed недавно, самый высокий отрицательный балл)
```

### Поведение правила Time

```
Reverse: false → Балл = временная метка (секунды с эпохи)
- Старый файл (2020 год): балл = 1577836800 (ниже) → быстрый уровень первым
- Новый файл (2025 год): балл = 1735689600 (выше) → медленный уровень последним

Reverse: true → Балл = -временная метка
- Новый файл (2025 год): балл = -1735689600 (ниже) → быстрый уровень первым
- Старый файл (2020 год): балл = -1577836800 (выше) → медленный уровень последним
```

---

## Пример 7: Тестирование с MockCapacity

**Сценарий:** Тестировать многоуровневое хранилище без реальных ограничений диска

### Конфигурация

```json
{
    "IterationLimit": 20,
    "LogLevel": "Debug",
    "TemporaryPath": "tmp",
    "Tiers": [
        {
            "target": 100,
            "path": "/tmp/test-hot",
            "MockCapacity": 2097152
        },
        {
            "target": 100,
            "path": "/tmp/test-cold",
            "MockCapacity": 10485760
        }
    ]
}
```

### Значения MockCapacity

| Уровень | MockCapacity | Реальный эквивалент |
|---------|--------------|---------------------|
| Hot | 2097152 (2МБ) | 2МБ раздел |
| Cold | 10485760 (10МБ) | 10МБ раздел |

### Тестовые файлы

```bash
# Создать тестовые файлы
dd if=/dev/zero of=/tmp/test-cold/small.bin bs=1K count=100
dd if=/dev/zero of=/tmp/test-cold/medium.bin bs=1K count=500
dd if=/dev/zero of=/tmp/test-cold/large.bin bs=1K count=1500

# Запустить tires
tires /tmp/test-config.json

# Проверить распределение
find /tmp/test-hot -name "*.bin"   # Должны быть малые файлы
find /tmp/test-cold -name "*.bin"  # Должны быть большие файлы
```

### Ожидаемый результат

**Поведение по умолчанию (нет FolderRules):** Малые файлы сортируются первыми → идут на горячий уровень

```
/tmp/test-hot/
├── small.bin    (100КБ)  ← Самый маленький, обработан первым → горячий уровень
└── medium.bin   (500КБ)  ← Средний, помещается в оставшееся место

/tmp/test-cold/
└── large.bin    (1,5МБ)  ← Самый большой, обработан последним → холодный уровень
```

---

## Пример 8: Рабочий процесс фото/видео редактора

**Сценарий:** Держать текущие проекты быстрыми, архивировать старую работу

### Конфигурация

```json
{
    "IterationLimit": 20,
    "LogLevel": "Information",
    "Tiers": [
        {"target": 80, "path": "/mnt/nvme"},
        {"target": 90, "path": "/mnt/sata"},
        {"target": 100, "path": "/mnt/archive"}
    ],
    "FolderRules": [
        {
            "PathPrefix": "current_project",
            "Priority": 100,
            "RuleType": "Ignore"
        },
        {
            "PathPrefix": "raw",
            "Priority": 60,
            "RuleType": "Name",
            "Pattern": ".dng"
        },
        {
            "PathPrefix": "exports",
            "Priority": 50,
            "RuleType": "Size",
            "Reverse": true
        },
        {
            "PathPrefix": "completed",
            "Priority": 30,
            "RuleType": "Time",
            "TimeType": "Modify",
            "Reverse": true
        }
    ]
}
```

### Рабочий процесс

```
/mnt/nvme/ (Самый быстрый)
├── current_project/    ← Никогда не перемещается (приоритет 100)
│   ├── active.psd
│   └── working.raw
└── cache/              ← Недавние файлы кэша

/mnt/sata/ (Быстрый)
├── raw/                ← .dng файлы (приоритет 60)
├── exports/            ← Большие экспорты (приоритет 50, Reverse: true)
└── recent/             ← Недавние завершённые проекты

/mnt/archive/ (Медленный)
├── exports/            ← Малые экспорты (обработаны последними)
└── completed/          ← Старые проекты (обратное время)
```

### Разбор приоритетов

| Папка | Правило | Приоритет | Эффект |
|-------|---------|----------|--------|
| current_project | Ignore | 100 | Никогда не перемещается |
| raw | Name (.dng) | 60 | RAW файлы → NVMe первыми |
| exports | Size (reverse) | 50 | **Большие экспорты → NVMe**, малые → архив |
| completed | Time (reverse) | 30 | **Новые проекты → NVMe**, старые → архив |

### Правило Size для экспортов

С `"RuleType": "Size", "Reverse": true`:
- **Большие экспорты** (4K видео, 500МБ+) → сортируются первыми → **остаются на NVMe**
- **Малые экспорты** (превью, 10МБ) → сортируются последними → перемещаются в архив

**Почему?** Большие файлы получают отрицательный балл (например, -500000000), малые файлы получают менее отрицательный балл (например, -10000000). Меньшие баллы идут на быстрые уровни.

---

## Смотрите также

- [Справочник конфигурации](../README.md#configuration)
- [Типы правил](../README.md#rules)
- [Служба Systemd](../README.md#systemd-service)
